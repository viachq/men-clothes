apiVersion: apps/v1
kind: Deployment
metadata:
  name: order-service
  namespace: ippt-microservices
  labels:
    app: order-service
    service: backend
spec:
  replicas: 2
  selector:
    matchLabels:
      app: order-service
  template:
    metadata:
      labels:
        app: order-service
        service: backend
    spec:
      containers:
      - name: order-service
        image: ghcr.io/viachq/ippt-microservices-order-service:latest
        imagePullPolicy: Always
        ports:
        - containerPort: 8003
          name: http
          protocol: TCP
        env:
        - name: JWT_SECRET_KEY
          valueFrom:
            secretKeyRef:
              name: ippt-microservices-secrets
              key: JWT_SECRET_KEY
        - name: DATABASE_URL
          valueFrom:
            configMapKeyRef:
              name: ippt-microservices-config
              key: DATABASE_URL_ORDER
        - name: SERVICE_NAME
          value: "order-service"
        - name: SERVICE_PORT
          valueFrom:
            configMapKeyRef:
              name: ippt-microservices-config
              key: SERVICE_PORT_ORDER
        - name: AUTH_SERVICE_URL
          valueFrom:
            configMapKeyRef:
              name: ippt-microservices-config
              key: AUTH_SERVICE_URL
        - name: CATALOG_SERVICE_URL
          valueFrom:
            configMapKeyRef:
              name: ippt-microservices-config
              key: CATALOG_SERVICE_URL
        volumeMounts:
        - name: order-db-storage
          mountPath: /app/data
        resources:
          requests:
            memory: "128Mi"
            cpu: "100m"
          limits:
            memory: "256Mi"
            cpu: "500m"
        livenessProbe:
          httpGet:
            path: /health
            port: 8003
          initialDelaySeconds: 30
          periodSeconds: 10
          timeoutSeconds: 5
          failureThreshold: 3
        readinessProbe:
          httpGet:
            path: /health
            port: 8003
          initialDelaySeconds: 10
          periodSeconds: 5
          timeoutSeconds: 3
          failureThreshold: 3
      volumes:
      - name: order-db-storage
        emptyDir: {}  # For production, use PersistentVolumeClaim
